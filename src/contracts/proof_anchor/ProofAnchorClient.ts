/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'
import { AppFactory as _AppFactory, AppFactoryAppClientParams, AppFactoryResolveAppClientByCreatorAndNameParams, AppFactoryDeployParams, AppFactoryParams, CreateSchema } from '@algorandfoundation/algokit-utils/types/app-factory'
import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'

export const APP_SPEC: Arc56Contract = {"name":"ProofAnchor","structs":{},"methods":[{"name":"submitProof","args":[{"type":"string","name":"proofHash","desc":"The cryptographic hash of the ZK proof (e.g., SHA-256)"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Anchors a ZK proof hash to the sender's account.\nEnsures that only the wallet owner can submit their own proof metadata.","events":[],"recommendations":{}},{"name":"getProof","args":[{"type":"address","name":"account","desc":"The account to query"}],"returns":{"type":"string","desc":"The anchored proof hash"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Retrieves the proof hash for a given account.","events":[],"recommendations":{}},{"name":"getTotalProofs","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Returns the total number of proofs anchored by this contract.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"ProofAnchor Contract\nImmutably stores ZK-KYC proof hashes bound to wallet addresses.","networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"totalProofs":{"keyType":"AVMString","valueType":"AVMUint64","key":"dHA=","desc":"Total number of proofs anchored on-chain"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"proofs":{"keyType":"address","valueType":"AVMString","desc":"Mapping of Wallet Address -> Proof Hash","prefix":"cA=="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[126],"errorMessage":"No proof found for this account"},{"pc":[25],"errorMessage":"OnCompletion must be NoOp"},{"pc":[65],"errorMessage":"OnCompletion must be NoOp && can only call when creating"},{"pc":[89],"errorMessage":"Proof hash cannot be empty"},{"pc":[102,146],"errorMessage":"check GlobalState exists"},{"pc":[73],"errorMessage":"invalid array length header"},{"pc":[81],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"},{"pc":[118],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"source":{"approval":"","clear":"I3ByYWdtYSB2ZXJzaW9uIDExCiNwcmFnbWEgdHlwZXRyYWNrIGZhbHNlCgovLyBAYWxnb3JhbmRmb3VuZGF0aW9uL2FsZ29yYW5kLXR5cGVzY3JpcHQvYmFzZS1jb250cmFjdC5kLnRzOjpCYXNlQ29udHJhY3QuY2xlYXJTdGF0ZVByb2dyYW0oKSAtPiB1aW50NjQ6Cm1haW46CiAgICBwdXNoaW50IDEgLy8gMQogICAgcmV0dXJuCg=="},"byteCode":{"approval":"CyACAQAmAwJ0cAFwBBUffHUxG0EAJDEZFEQxGESCAwTVKu+zBAgQdFkEH2zwATYaAI4DAAsANQBWADEZFDEYFBBEIkM2GgFJI1mBAghLARUSRFcCAEmAABNEKTEAUEm8SEy/IyhlRCIIKExnIkM2GgFJFYEgEkQpTFBJvUUBRL5ISRUWVwYCTFAqTFCwIkMjKGVEFipMULAiQw==","clear":"C4EBQw=="},"events":[],"templateVariables":{}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the ProofAnchor contract
 */
export type ProofAnchorArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'submitProof(string)void': {
      /**
       * The cryptographic hash of the ZK proof (e.g., SHA-256)
       */
      proofHash: string
    }
    'getProof(address)string': {
      /**
       * The account to query
       */
      account: string
    }
    'getTotalProofs()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'submitProof(string)void': [proofHash: string]
    'getProof(address)string': [account: string]
    'getTotalProofs()uint64': []
  }
}

/**
 * The return type for each method
 */
export type ProofAnchorReturns = {
  'submitProof(string)void': void
  'getProof(address)string': string
  'getTotalProofs()uint64': bigint
}

/**
 * Defines the types of available calls and state of the ProofAnchor smart contract.
 */
export type ProofAnchorTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'submitProof(string)void' | 'submitProof', {
      argsObj: ProofAnchorArgs['obj']['submitProof(string)void']
      argsTuple: ProofAnchorArgs['tuple']['submitProof(string)void']
      returns: ProofAnchorReturns['submitProof(string)void']
    }>
    & Record<'getProof(address)string' | 'getProof', {
      argsObj: ProofAnchorArgs['obj']['getProof(address)string']
      argsTuple: ProofAnchorArgs['tuple']['getProof(address)string']
      /**
       * The anchored proof hash
       */
      returns: ProofAnchorReturns['getProof(address)string']
    }>
    & Record<'getTotalProofs()uint64' | 'getTotalProofs', {
      argsObj: ProofAnchorArgs['obj']['getTotalProofs()uint64']
      argsTuple: ProofAnchorArgs['tuple']['getTotalProofs()uint64']
      returns: ProofAnchorReturns['getTotalProofs()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        /**
         * Total number of proofs anchored on-chain
         */
        totalProofs: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        /**
         * Mapping of Wallet Address -> Proof Hash
         */
        proofs: Map<string, string>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type ProofAnchorSignatures = keyof ProofAnchorTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type ProofAnchorNonVoidMethodSignatures = keyof ProofAnchorTypes['methods'] extends infer T ? T extends keyof ProofAnchorTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the ProofAnchor smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends ProofAnchorSignatures> = ProofAnchorTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the ProofAnchor smart contract to the method's return type
 */
export type MethodReturn<TSignature extends ProofAnchorSignatures> = ProofAnchorTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = ProofAnchorTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = ProofAnchorTypes['state']['box']['keys']


/**
 * Defines supported create method params for this smart contract
 */
export type ProofAnchorCreateCallParams =
  | Expand<AppClientBareCallParams & {method?: never} & {onComplete?: OnApplicationComplete.NoOpOC} & CreateSchema>
/**
 * Defines arguments required for the deploy method.
 */
export type ProofAnchorDeployParams = Expand<Omit<AppFactoryDeployParams, 'createParams' | 'updateParams' | 'deleteParams'> & {
  /**
   * Create transaction parameters to use if a create needs to be issued as part of deployment; use `method` to define ABI call (if available) or leave out for a bare call (if available)
   */
  createParams?: ProofAnchorCreateCallParams
}>


/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the ProofAnchor smart contract
 */
export abstract class ProofAnchorParamsFactory {
  /**
   * Constructs a no op call for the submitProof(string)void ABI method
   *
  * Anchors a ZK proof hash to the sender's account.
  Ensures that only the wallet owner can submit their own proof metadata.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static submitProof(params: CallParams<ProofAnchorArgs['obj']['submitProof(string)void'] | ProofAnchorArgs['tuple']['submitProof(string)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'submitProof(string)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.proofHash],
    }
  }
  /**
   * Constructs a no op call for the getProof(address)string ABI method
   *
   * Retrieves the proof hash for a given account.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getProof(params: CallParams<ProofAnchorArgs['obj']['getProof(address)string'] | ProofAnchorArgs['tuple']['getProof(address)string']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getProof(address)string' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.account],
    }
  }
  /**
   * Constructs a no op call for the getTotalProofs()uint64 ABI method
   *
   * Returns the total number of proofs anchored by this contract.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getTotalProofs(params: CallParams<ProofAnchorArgs['obj']['getTotalProofs()uint64'] | ProofAnchorArgs['tuple']['getTotalProofs()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'getTotalProofs()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A factory to create and deploy one or more instance of the ProofAnchor smart contract and to create one or more app clients to interact with those (or other) app instances
 */
export class ProofAnchorFactory {
  /**
   * The underlying `AppFactory` for when you want to have more flexibility
   */
  public readonly appFactory: _AppFactory

  /**
   * Creates a new instance of `ProofAnchorFactory`
   *
   * @param params The parameters to initialise the app factory with
   */
  constructor(params: Omit<AppFactoryParams, 'appSpec'>) {
    this.appFactory = new _AppFactory({
      ...params,
      appSpec: APP_SPEC,
    })
  }
  
  /** The name of the app (from the ARC-32 / ARC-56 app spec or override). */
  public get appName() {
    return this.appFactory.appName
  }
  
  /** The ARC-56 app spec being used */
  get appSpec() {
    return APP_SPEC
  }
  
  /** A reference to the underlying `AlgorandClient` this app factory is using. */
  public get algorand(): AlgorandClient {
    return this.appFactory.algorand
  }
  
  /**
   * Returns a new `AppClient` client for an app instance of the given ID.
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public getAppClientById(params: AppFactoryAppClientParams) {
    return new ProofAnchorClient(this.appFactory.getAppClientById(params))
  }
  
  /**
   * Returns a new `AppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public async getAppClientByCreatorAndName(
    params: AppFactoryResolveAppClientByCreatorAndNameParams,
  ) {
    return new ProofAnchorClient(await this.appFactory.getAppClientByCreatorAndName(params))
  }

  /**
   * Idempotently deploys the ProofAnchor smart contract.
   *
   * @param params The arguments for the contract calls and any additional parameters for the call
   * @returns The deployment result
   */
  public async deploy(params: ProofAnchorDeployParams = {}) {
    const result = await this.appFactory.deploy({
      ...params,
    })
    return { result: result.result, appClient: new ProofAnchorClient(result.appClient) }
  }

  /**
   * Get parameters to create transactions (create and deploy related calls) for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the ProofAnchor smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The params for a create call
       */
      bare: (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        return this.appFactory.params.bare.create(params)
      },
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the ProofAnchor smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The transaction for a create call
       */
      bare: (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        return this.appFactory.createTransaction.bare.create(params)
      },
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the ProofAnchor smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The create result
       */
      bare: async (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        const result = await this.appFactory.send.bare.create(params)
        return { result: result.result, appClient: new ProofAnchorClient(result.appClient) }
      },
    },

  }

}
/**
 * A client to make calls to the ProofAnchor smart contract
 */
export class ProofAnchorClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `ProofAnchorClient`
   *
   * @param appClient An `AppClient` instance which has been created with the ProofAnchor app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `ProofAnchorClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }

  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends ProofAnchorNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }

  /**
   * Returns a new `ProofAnchorClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<ProofAnchorClient> {
    return new ProofAnchorClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `ProofAnchorClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<ProofAnchorClient> {
    return new ProofAnchorClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the ProofAnchor smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the ProofAnchor smart contract using the `submitProof(string)void` ABI method.
     *
    * Anchors a ZK proof hash to the sender's account.
    Ensures that only the wallet owner can submit their own proof metadata.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    submitProof: (params: CallParams<ProofAnchorArgs['obj']['submitProof(string)void'] | ProofAnchorArgs['tuple']['submitProof(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ProofAnchorParamsFactory.submitProof(params))
    },

    /**
     * Makes a call to the ProofAnchor smart contract using the `getProof(address)string` ABI method.
     *
     * Retrieves the proof hash for a given account.
     *
     * @param params The params for the smart contract call
     * @returns The call params: The anchored proof hash
     */
    getProof: (params: CallParams<ProofAnchorArgs['obj']['getProof(address)string'] | ProofAnchorArgs['tuple']['getProof(address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(ProofAnchorParamsFactory.getProof(params))
    },

    /**
     * Makes a call to the ProofAnchor smart contract using the `getTotalProofs()uint64` ABI method.
     *
     * Returns the total number of proofs anchored by this contract.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getTotalProofs: (params: CallParams<ProofAnchorArgs['obj']['getTotalProofs()uint64'] | ProofAnchorArgs['tuple']['getTotalProofs()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(ProofAnchorParamsFactory.getTotalProofs(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the ProofAnchor smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the ProofAnchor smart contract using the `submitProof(string)void` ABI method.
     *
    * Anchors a ZK proof hash to the sender's account.
    Ensures that only the wallet owner can submit their own proof metadata.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    submitProof: (params: CallParams<ProofAnchorArgs['obj']['submitProof(string)void'] | ProofAnchorArgs['tuple']['submitProof(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ProofAnchorParamsFactory.submitProof(params))
    },

    /**
     * Makes a call to the ProofAnchor smart contract using the `getProof(address)string` ABI method.
     *
     * Retrieves the proof hash for a given account.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: The anchored proof hash
     */
    getProof: (params: CallParams<ProofAnchorArgs['obj']['getProof(address)string'] | ProofAnchorArgs['tuple']['getProof(address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(ProofAnchorParamsFactory.getProof(params))
    },

    /**
     * Makes a call to the ProofAnchor smart contract using the `getTotalProofs()uint64` ABI method.
     *
     * Returns the total number of proofs anchored by this contract.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getTotalProofs: (params: CallParams<ProofAnchorArgs['obj']['getTotalProofs()uint64'] | ProofAnchorArgs['tuple']['getTotalProofs()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(ProofAnchorParamsFactory.getTotalProofs(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the ProofAnchor smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the ProofAnchor smart contract using the `submitProof(string)void` ABI method.
     *
    * Anchors a ZK proof hash to the sender's account.
    Ensures that only the wallet owner can submit their own proof metadata.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    submitProof: async (params: CallParams<ProofAnchorArgs['obj']['submitProof(string)void'] | ProofAnchorArgs['tuple']['submitProof(string)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ProofAnchorParamsFactory.submitProof(params))
      return {...result, return: result.return as unknown as (undefined | ProofAnchorReturns['submitProof(string)void'])}
    },

    /**
     * Makes a call to the ProofAnchor smart contract using the `getProof(address)string` ABI method.
     *
     * Retrieves the proof hash for a given account.
     *
     * @param params The params for the smart contract call
     * @returns The call result: The anchored proof hash
     */
    getProof: async (params: CallParams<ProofAnchorArgs['obj']['getProof(address)string'] | ProofAnchorArgs['tuple']['getProof(address)string']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(ProofAnchorParamsFactory.getProof(params))
      return {...result, return: result.return as unknown as (undefined | ProofAnchorReturns['getProof(address)string'])}
    },

    /**
     * Makes a call to the ProofAnchor smart contract using the `getTotalProofs()uint64` ABI method.
     *
     * Returns the total number of proofs anchored by this contract.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getTotalProofs: async (params: CallParams<ProofAnchorArgs['obj']['getTotalProofs()uint64'] | ProofAnchorArgs['tuple']['getTotalProofs()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(ProofAnchorParamsFactory.getTotalProofs(params))
      return {...result, return: result.return as unknown as (undefined | ProofAnchorReturns['getTotalProofs()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new ProofAnchorClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current ProofAnchor app
   */
  state = {
    /**
     * Methods to access global state for the current ProofAnchor app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          totalProofs: result.totalProofs,
        }
      },
      /**
       * Get the current value of the totalProofs key in global state
       */
      totalProofs: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("totalProofs")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current ProofAnchor app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the proofs map in box state
       */
      proofs: {
        /**
         * Get all current values of the proofs map in box state
         */
        getMap: async (): Promise<Map<string, string>> => { return (await this.appClient.state.box.getMap("proofs")) as Map<string, string> },
        /**
         * Get a current value of the proofs map by key from box state
         */
        value: async (key: string): Promise<string | undefined> => { return await this.appClient.state.box.getMapValue("proofs", key) as string | undefined },
      },
    },
  }

  public newGroup(): ProofAnchorComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a submitProof(string)void method call against the ProofAnchor contract
       */
      submitProof(params: CallParams<ProofAnchorArgs['obj']['submitProof(string)void'] | ProofAnchorArgs['tuple']['submitProof(string)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.submitProof(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a getProof(address)string method call against the ProofAnchor contract
       */
      getProof(params: CallParams<ProofAnchorArgs['obj']['getProof(address)string'] | ProofAnchorArgs['tuple']['getProof(address)string']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getProof(params)))
        resultMappers.push((v) => client.decodeReturnValue('getProof(address)string', v))
        return this
      },
      /**
       * Add a getTotalProofs()uint64 method call against the ProofAnchor contract
       */
      getTotalProofs(params: CallParams<ProofAnchorArgs['obj']['getTotalProofs()uint64'] | ProofAnchorArgs['tuple']['getTotalProofs()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getTotalProofs(params)))
        resultMappers.push((v) => client.decodeReturnValue('getTotalProofs()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the ProofAnchor contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as ProofAnchorComposer
  }
}
export type ProofAnchorComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the submitProof(string)void ABI method.
   *
  * Anchors a ZK proof hash to the sender's account.
  Ensures that only the wallet owner can submit their own proof metadata.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  submitProof(params?: CallParams<ProofAnchorArgs['obj']['submitProof(string)void'] | ProofAnchorArgs['tuple']['submitProof(string)void']>): ProofAnchorComposer<[...TReturns, ProofAnchorReturns['submitProof(string)void'] | undefined]>

  /**
   * Calls the getProof(address)string ABI method.
   *
   * Retrieves the proof hash for a given account.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getProof(params?: CallParams<ProofAnchorArgs['obj']['getProof(address)string'] | ProofAnchorArgs['tuple']['getProof(address)string']>): ProofAnchorComposer<[...TReturns, ProofAnchorReturns['getProof(address)string'] | undefined]>

  /**
   * Calls the getTotalProofs()uint64 ABI method.
   *
   * Returns the total number of proofs anchored by this contract.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getTotalProofs(params?: CallParams<ProofAnchorArgs['obj']['getTotalProofs()uint64'] | ProofAnchorArgs['tuple']['getTotalProofs()uint64']>): ProofAnchorComposer<[...TReturns, ProofAnchorReturns['getTotalProofs()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the ProofAnchor smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): ProofAnchorComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): ProofAnchorComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<ProofAnchorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<ProofAnchorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<ProofAnchorComposerResults<TReturns> & { simulateResponse: modelsv2.SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<ProofAnchorComposerResults<TReturns>>
}
export type ProofAnchorComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

